<nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
    <div class="container">
        <div class="row">
            <a href="{{ path('index') }}" class="navbar-brand btn btn-light">
                <img src="{{ asset('img/logo.png') }}" alt="logo" height="25"/>
            </a>
            {% if app.user %}
                <button class="btn btn-light" id="menu-toggle"><i data-feather="menu"></i></button>
            {% endif %}
        </div>
        <div class="row">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                {% if is_granted('ROLE_STUDENT') %}
                    <a href="{{ path('desktop') }}" class="btn btn-outline-primary mx-1">
                        <i data-feather="folder" width="15"></i>
                        Mon bureau
                    </a>
                {% endif %}

                <a href="{{ path("member_index") }}" class="btn btn-primary mx-1">
                    <i data-feather="user" width="15"></i> Mon espace
                </a>
                <a href="{{ path('app_logout') }}"
                   data-toggle="tooltip"
                   title="Déconnexion"
                   class="btn btn-secondary mx-1">
                    <i data-feather="log-out" width="15"></i>
                </a>
            {% else %}
                <button class="btn btn-outline-primary mr-2"
                        data-toggle="modal"
                        data-target="#logInModal">
                    Connexion
                </button>
                <button class="btn btn-primary"
                        data-toggle="modal"
                        data-target="#registerModal">
                    Inscription
                </button>
            {% endif %}
        </div>
    </div>
</nav>